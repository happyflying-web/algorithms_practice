#include<iostream>
#include<string>
#include<algorithm>
using namespace std;
class Solution {
public:
    char findTheDifference(string s, string t) {
        string temp1;
        string temp2;
        sort(s.begin(),s.end());
        sort(t.begin(),t.end());
        
    }
};